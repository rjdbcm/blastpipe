rule pip
  command = pip install -r $in

rule copy
  command = cp -Tr $out $in

build dist: pip dist-requirements.txt

build docs: pip docs-requirements.txt

build test: pip test-requirements.txt

build lint: pip lint-requirements.txt

build vscode:  copy ../.vscode

build dev: phony
